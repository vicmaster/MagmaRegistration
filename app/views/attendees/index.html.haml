%h1 Attendees list
- counter = 1
%table{id: 'attendees', class: "table"}
  %thead
    %tr
      %th #
      %th
        = t("activerecord.attributes.student.name", :default => t("activerecord.labels.name", :default => "Name"))
      %th Correo
      %th Twitter
      %th Lugar
      %th Empresa
      %th Genero
      %th acciones
  %tbody
    - @attendees.each do |attendee|
      %tr
        %td= counter
        - counter = counter + 1
        %td= link_to attendee.name, attendee_path(attendee)
        %td= attendee.email
        %td= attendee.twitter
        %td= attendee.place
        %td= attendee.enterprise
        %td= attendee.gender
        %td
          = link_to "Show", attendee_path(attendee)
          |
          = link_to "Edit", edit_attendee_path(attendee)
          |
          = link_to "Destroy", attendee_path(attendee), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}"