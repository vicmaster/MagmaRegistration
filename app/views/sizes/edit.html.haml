= form_for @size, :url => size_path(@size), :html => { :class => "edit_size", :id => "edit_size" } do |f|
  %input{:name => "_method", :type => "hidden", :value =>"put"}
  = render :partial => "form", :locals => {:f => f}
